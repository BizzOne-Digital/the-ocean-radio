# Live stream URL (optional — player shows "coming soon" until set)
NEXT_PUBLIC_RADIO_STREAM_URL=

# Contact form delivery (optional — at least one when ready for production)
CONTACT_WEBHOOK_URL=
RESEND_API_KEY=
